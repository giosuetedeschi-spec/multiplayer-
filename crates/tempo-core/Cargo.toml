[package]
name = "tempo-core"
description = "The tempo world arena, snapshots and delta replication."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
publish = false

[dependencies]
tempo-fixed.workspace = true
tempo-wire.workspace = true
blake3 = { version = "1", default-features = false }

[dev-dependencies]
proptest = "1"

[lints]
workspace = true
