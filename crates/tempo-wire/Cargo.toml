[package]
name = "tempo-wire"
description = "Bit-packed wire codec, quantization and schema canonicalisation for tempo."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
publish = false

[dependencies]
tempo-fixed.workspace = true
blake3 = { version = "1", default-features = false }

[dev-dependencies]
proptest = "1"

[lints]
workspace = true
